import 'package:flutter/material.dart';

class GraphPreview extends StatelessWidget {
  const GraphPreview({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 200,
      child: Placeholder(),
    );
  }
}
